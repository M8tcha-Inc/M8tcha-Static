---
const steps = [
  {
    id: '01',
    title: 'Show up as you',
    body: 'Build a profile that actually sounds like you - photos, vibes, and the stuff that makes people lean in.',
  },
  {
    id: '02',
    title: 'Discover nearby',
    body: 'Browse people around you. Pass, like, or super-like when something clicks.',
  },
  {
    id: '03',
    title: 'Match & message',
    body: 'When it’s mutual, start the conversation. No pressure to perform - just chemistry.',
  },
  {
    id: '04',
    title: 'Enter a Room',
    body: 'Join live audio or video hangouts. Hear the laugh. Feel the energy. Decide offline from there.',
  },
];
---

<section class="process" id="process">
  <div class="wrap">
    <div class="process__head reveal">
      <div>
        <p class="eyebrow">How it works</p>
        <h2>Four beats from <span class="accent-pink">hello</span> to hangout.</h2>
      </div>
      <a class="btn btn-ghost" href="#download">Start free</a>
    </div>

    <ol class="steps">
      {
        steps.map((step, i) => (
          <li class={`step reveal reveal-delay-${(i % 3) + 1}`}>
            <span class="step__id">{step.id}</span>
            <h3>{step.title}</h3>
            <p>{step.body}</p>
          </li>
        ))
      }
    </ol>
  </div>
</section>

<style>
  .process {
    position: relative;
    z-index: 1;
    padding: var(--section-space) 0;
    background: color-mix(in srgb, var(--matcha-muted) 55%, var(--bg));
  }

  .process__head {
    display: flex;
    flex-wrap: wrap;
    align-items: end;
    justify-content: space-between;
    gap: 1.5rem;
    margin-bottom: clamp(2rem, 5vw, 3.75rem);
  }

  .eyebrow {
    color: var(--matcha-bright);
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 0.82rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    margin-bottom: 0.85rem;
  }

  h2 {
    font-size: var(--font-size-h2);
    max-width: 14ch;
  }

  .steps {
    display: grid;
    gap: 1rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .step {
    padding: 1.5rem 1.35rem;
    border-radius: var(--radius);
    border: 1.5px solid var(--border);
    background: var(--bg-elevated);
  }

  .step__id {
    display: inline-block;
    margin-bottom: 1.1rem;
    color: var(--pink);
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 0.95rem;
  }

  h3 {
    font-size: var(--font-size-h3);
    margin-bottom: 0.5rem;
  }

  .step p {
    color: var(--text-muted);
  }

  @media (min-width: 800px) {
    .steps {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1100px) {
    .steps {
      grid-template-columns: repeat(4, 1fr);
    }
  }
</style>
